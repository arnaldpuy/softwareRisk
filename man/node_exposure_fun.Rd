% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node_exposure_fun.R
\name{node_exposure_fun}
\alias{node_exposure_fun}
\title{Path-aware node criticality (risk load)}
\usage{
node_exposure_fun(all_paths_out)
}
\arguments{
\item{all_paths_out}{A list produced by \code{\link[=all_paths_fun]{all_paths_fun()}} with elements
\code{nodes} and \code{paths}. \code{nodes} must contain \code{name} and \code{risk_score};
\code{paths} must contain \code{path_nodes} and \code{path_risk_score}.}
}
\value{
A tibble with one row per node appearing on at least one path,
sorted by decreasing \code{risk_load}, with columns \code{name}, \code{risk_score},
\code{n_paths}, \code{path_share}, \code{risk_load}, \code{risk_load_share},
\code{mean_path_risk}, \code{rank_risk_load} and \code{rank_risk_score}. Nodes on no
path (e.g., disconnected helpers) are omitted. If \code{all_paths_out$paths}
is empty, an empty tibble with these columns is returned.
}
\description{
Summarizes, for each node, its participation in the entry-to-sink paths
enumerated by \code{\link[=all_paths_fun]{all_paths_fun()}}. Whereas the node risk score captures how
error-prone a function is in isolation, the \emph{risk load} captures how many
risky execution chains depend on it: a function of moderate complexity can
still be critical if most high-risk paths route through it.
}
\details{
For node \eqn{v} the function computes:
\itemize{
\item \code{n_paths}: the number of paths that contain \eqn{v},
\item \code{path_share}: \code{n_paths} divided by the total number of paths,
\item \code{risk_load}: \eqn{\sum_{k : v \in k} P_k}, the sum of
\code{path_risk_score} over the paths containing \eqn{v},
\item \code{risk_load_share}: \code{risk_load} divided by \eqn{\sum_k P_k},
\item \code{mean_path_risk}: the mean \code{path_risk_score} of the paths
containing \eqn{v}.
}

The output also reports the rank of each node by \code{risk_load} and by its
standalone \code{risk_score}, so that structurally exposed functions can be
told apart from merely complex ones: a node ranking much higher on
\code{risk_load} than on \code{risk_score} is a chokepoint rather than a hotspot.
}
\examples{
data(synthetic_graph)
out <- all_paths_fun(graph = synthetic_graph, alpha = 0.6, beta = 0.3,
                     gamma = 0.1, complexity_col = "cyclo")
node_exposure_fun(out)

}
\seealso{
\code{\link[=path_fix_heatmap]{path_fix_heatmap()}} for the per-path effect of fixing single
nodes, and \code{\link[=fix_portfolio_fun]{fix_portfolio_fun()}} for a budgeted selection of fixes.
}
