% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_graph_fun.R
\name{call_graph_fun}
\alias{call_graph_fun}
\title{Build a call graph from an R package or a directory of R scripts}
\usage{
call_graph_fun(pkg = NULL, dir = NULL, exclude = NULL, keep_self_loops = FALSE)
}
\arguments{
\item{pkg}{Character scalar. Name of an installed package whose namespace
functions (exported and internal) are analyzed. Exactly one of \code{pkg} or
\code{dir} must be supplied.}

\item{dir}{Character scalar. Path to a directory containing \code{.R} files.
Files are parsed (searched recursively) and all top-level
\verb{name <- function(...)} definitions (also with \code{=} or \verb{<<-}) are
collected. The code is never executed: function literals are only
parsed and turned into closures.}

\item{exclude}{Optional character vector of function names to drop from
the graph (e.g., generated or vendored code).}

\item{keep_self_loops}{Logical. Keep self-recursion edges (a function
calling itself)? These edges do not affect the simple-path enumeration
of \code{\link[=all_paths_fun]{all_paths_fun()}} but change the in-degree. Default \code{FALSE}.}
}
\value{
A directed \code{tidygraph::tbl_graph} with node attributes \code{name} and
\code{cyclo}, ready to be passed to \code{\link[=all_paths_fun]{all_paths_fun()}}.
}
\description{
Constructs the directed call graph required by \code{\link[=all_paths_fun]{all_paths_fun()}}
automatically, so that no manual preparation of edge lists or complexity
spreadsheets is needed for \code{R} code. Each node is a function, each edge
\code{from -> to} is a call from function \code{from} (caller) to function \code{to}
(callee), and each node carries a \code{cyclo} attribute with its cyclomatic
complexity.
}
\details{
Call detection relies on \code{codetools::findGlobals()}, i.e., on static
analysis of each function body. Two limitations follow: (i) calls built
at run time (e.g., \code{do.call(paste0("f", i), ...)}, \code{get()}, method
dispatch) cannot be detected; (ii) a function of the set that is merely
\emph{referenced} (e.g., passed to \code{lapply()}) is treated as called, since
in a dependency sense the caller relies on it.

Cyclomatic complexity is computed internally as \eqn{1} plus one unit per
decision point (\code{if}, \code{for}, \code{while}, \code{repeat}, \code{&&}, \code{||}, and each
\code{switch()} alternative beyond the first), following McCabe (1976).
}
\examples{
# build the call graph of a directory of R scripts
td <- file.path(tempdir(), "cg_example")
dir.create(td, showWarnings = FALSE)
writeLines(c(
  "load_data <- function(x) x",
  "clean_data <- function(x) load_data(x)",
  "calc_scores <- function(x) if (length(x) > 0) mean(x) else 0",
  "compute_risk <- function(x) calc_scores(clean_data(x))"
), file.path(td, "model.R"))

g <- call_graph_fun(dir = td)
g

# the result feeds directly into the pipeline
out <- all_paths_fun(g, alpha = 0.6, beta = 0.3, gamma = 0.1)
out$paths

# build the call graph of an installed package
\donttest{
g_pkg <- call_graph_fun(pkg = "softwareRisk")
g_pkg
}

}
\references{
McCabe, T. J. (1976). \emph{A Complexity Measure}.
IEEE Transactions on Software Engineering, SE-2(4), 308--320.
doi:10.1109/TSE.1976.233837
}
\seealso{
\code{\link[=read_call_graph]{read_call_graph()}} to import a call graph prepared outside \code{R}
(e.g., for Fortran, C or Python models).
}
