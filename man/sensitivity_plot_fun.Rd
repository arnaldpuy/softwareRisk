% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_plot_fun.R
\name{sensitivity_plot_fun}
\alias{sensitivity_plot_fun}
\title{Plot Sobol' sensitivity indices of the node risk scores}
\usage{
sensitivity_plot_fun(ua_sa_out, nodes = NULL, index = c("both", "Si", "Ti"))
}
\arguments{
\item{ua_sa_out}{A list returned by \code{\link[=uncertainty_fun]{uncertainty_fun()}} with element
\code{nodes} containing a \code{sensitivity_analysis} list-column.}

\item{nodes}{Optional character vector of node names to display
individually. Default \code{NULL} (aggregate view across all nodes).}

\item{index}{Character scalar: \code{"both"} (default), \code{"Si"} (first-order)
or \code{"Ti"} (total-order). Which indices to display.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Visualizes the Sobol' indices stored in the \code{sensitivity_analysis}
list-column returned by \code{\link[=uncertainty_fun]{uncertainty_fun()}}, showing how much of the
variance in the node risk scores is driven by each parameter of the risk
definition (the weights \eqn{\alpha}, \eqn{\beta}, \eqn{\gamma} and the
power-mean exponent \eqn{p}).
}
\details{
Two displays are available:
\itemize{
\item With \code{nodes = NULL} (default), the distribution of the indices
across \emph{all} nodes is shown as one boxplot per parameter, answering
the system-level question of which assumption dominates the risk
ranking overall.
\item With \code{nodes} set to a character vector of node names, the indices
of those nodes are shown as dodged bars with their confidence
intervals, one panel per node.
}

Nodes whose risk score does not vary across draws (e.g., all metrics
zero) yield non-finite indices; these are dropped with a message.
}
\examples{
\donttest{
data(synthetic_graph)
out <- all_paths_fun(graph = synthetic_graph, alpha = 0.6, beta = 0.3,
                     gamma = 0.1, complexity_col = "cyclo")
results <- uncertainty_fun(all_paths_out = out, N = 2^7, order = "first")

# system-level view: which assumption drives the risk scores?
sensitivity_plot_fun(results)

# node-level view
sensitivity_plot_fun(results, nodes = results$nodes$name[1:4])
}

}
\references{
Puy, A., Lo Piano, S., Saltelli, A., and Levin, S. A. (2022).
\emph{sensobol: An R Package to Compute Variance-Based Sensitivity Indices}.
Journal of Statistical Software, 102(5), 1--37.
doi:10.18637/jss.v102.i05
}
\seealso{
\code{\link[=uncertainty_fun]{uncertainty_fun()}} to generate the indices and the
\pkg{sensobol} package (Puy et al. 2022) for their estimation.
}
