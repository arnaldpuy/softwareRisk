% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_robustness_fun.R
\name{rank_robustness_fun}
\alias{rank_robustness_fun}
\title{Robustness of the risk ranking under uncertainty}
\usage{
rank_robustness_fun(ua_sa_out, top_k = 10, what = c("paths", "nodes"))
}
\arguments{
\item{ua_sa_out}{A list returned by \code{\link[=uncertainty_fun]{uncertainty_fun()}} with elements
\code{nodes} and \code{paths}, each containing an \code{uncertainty_analysis}
list-column of numeric draws.}

\item{top_k}{Integer. Size of the top set whose membership is tracked.
Clamped to the number of items with a message if larger. Default \code{10}.}

\item{what}{Character scalar, \code{"paths"} (default) or \code{"nodes"}.}
}
\value{
A list with:
\itemize{
\item \code{summary}: a tibble with one row per item, sorted by decreasing
\code{top_k_prob}, with columns \code{id} (path \code{path_id} or node \code{name}),
\code{path_str} (paths only), \code{mean_risk}, \code{top_k_prob}, \code{rank_median},
\code{rank_q05} and \code{rank_q95}.
\item \code{consensus_correlation}: mean Spearman correlation between each
draw and the consensus risk values.
\item \code{top_k}, \code{what}: the settings used.
}
}
\description{
Quantifies how stable the identification of the top-\eqn{k} riskiest paths
(or nodes) is across the uncertainty draws produced by \code{\link[=uncertainty_fun]{uncertainty_fun()}}.
Every draw corresponds to one plausible definition of risk (one sampled
combination of the weights \eqn{\alpha, \beta, \gamma} and the exponent
\eqn{p}); ranking the paths within each draw therefore shows which paths
are flagged as risky \emph{regardless} of how risk is weighted and which ones
enter the top-\eqn{k} only under specific assumptions.
}
\details{
For each item (path or node) the function reports the probability of
belonging to the top-\eqn{k} across draws and the median and the 5th and
95th percentiles of its rank (rank 1 = riskiest; ties share the minimum
rank). As a global summary it also reports the mean Spearman correlation
between the risk values of each draw and the consensus (mean across
draws) risk values: values close to 1 indicate that the ranking barely
responds to the risk definition, values well below 1 that conclusions
depend on it.

Draws that are \code{NA} for an item are treated as never placing that item in
the top-\eqn{k}.
}
\examples{
\donttest{
data(synthetic_graph)
out <- all_paths_fun(graph = synthetic_graph, alpha = 0.6, beta = 0.3,
                     gamma = 0.1, complexity_col = "cyclo")
results <- uncertainty_fun(all_paths_out = out, N = 2^7, order = "first")

rr <- rank_robustness_fun(results, top_k = 10, what = "paths")
rr$summary
rr$consensus_correlation
}

}
\seealso{
\code{\link[=rank_robustness_plot]{rank_robustness_plot()}} to visualize the result and
\code{\link[=uncertainty_fun]{uncertainty_fun()}} to generate the draws.
}
