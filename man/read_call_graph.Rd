% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_graph_fun.R
\name{read_call_graph}
\alias{read_call_graph}
\title{Import a call graph from edge-list and complexity tables}
\usage{
read_call_graph(
  edges,
  metrics,
  from_col = "from",
  to_col = "to",
  name_col = "name",
  complexity_col = "cyclo"
)
}
\arguments{
\item{edges}{A data.frame (or path to a \code{.csv} file) with one row per
function call. Must contain the columns given by \code{from_col} (caller)
and \code{to_col} (callee).}

\item{metrics}{A data.frame (or path to a \code{.csv} file) with one row per
function. Must contain the columns given by \code{name_col} (function name)
and \code{complexity_col} (cyclomatic complexity).}

\item{from_col, to_col}{Character scalars. Names of the caller and callee
columns in \code{edges}. Defaults \code{"from"} and \code{"to"}.}

\item{name_col}{Character scalar. Name of the function-name column in
\code{metrics}. Default \code{"name"}.}

\item{complexity_col}{Character scalar. Name of the complexity column in
\code{metrics}. Default \code{"cyclo"}. The column keeps this name in the
returned graph, matching the default of \code{\link[=all_paths_fun]{all_paths_fun()}}.}
}
\value{
A directed \code{tidygraph::tbl_graph} with node attributes \code{name} and
\code{complexity_col}, ready to be passed to \code{\link[=all_paths_fun]{all_paths_fun()}}.
}
\description{
Builds the directed call graph required by \code{\link[=all_paths_fun]{all_paths_fun()}} from the two
datasets described in the package vignette: an edge list of function calls
and a table of per-function cyclomatic complexity. The input is validated
so that common preparation errors (misspelled columns, functions missing
from the complexity table, duplicated or non-numeric entries) fail early
with an informative message. This is the recommended entry point for
models written in languages other than \code{R} (e.g., Fortran, C, Python),
whose edge lists and complexity values are extracted with external tools.
}
\examples{
calls_df <- data.frame(
  from = c("clean_data", "compute_risk", "compute_risk", "calc_scores"),
  to   = c("load_data", "clean_data", "calc_scores", "trim_mean")
)
cyclo_df <- data.frame(
  name  = c("clean_data", "load_data", "compute_risk", "calc_scores",
            "trim_mean"),
  cyclo = c(6, 3, 12, 5, 2)
)

g <- read_call_graph(edges = calls_df, metrics = cyclo_df)
g

}
\seealso{
\code{\link[=call_graph_fun]{call_graph_fun()}} to build the graph automatically from \code{R}
source code.
}
